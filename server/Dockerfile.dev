# Dockerfile para desenvolvimento (usa npm install para hot reload)
FROM node:20-alpine

# Definir diretório de trabalho
WORKDIR /app

# Copiar arquivos de dependências
COPY package*.json ./

# Instalar todas as dependências (incluindo dev para nodemon)
RUN npm install

# Copiar código da aplicação
COPY . .

# Expor porta do servidor
EXPOSE 8000

# Em desenvolvimento, usa nodemon via docker-compose.yml command override
# Em produção, use Dockerfile.prod que usa npm start
CMD ["npm", "start"]

