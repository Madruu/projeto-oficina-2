# Porta do servidor
PORT=8000

# Ambiente (development, production, test)
NODE_ENV=development

# MongoDB Configuration (via Docker Compose)
# O MongoDB está rodando como serviço Docker, então use o nome do serviço "mongodb"
# Credenciais padrão (ajuste se necessário):
MONGO_ROOT_USERNAME=admin
MONGO_ROOT_PASSWORD=password
MONGO_DATABASE=oficina2

# A URI será montada automaticamente pelo docker-compose.yml
# MongoDB Docker (padrão para desenvolvimento)
MONGO_URI=mongodb://admin:password@mongodb:27017/oficina2?authSource=admin

# ============================================
# PRODUÇÃO - MongoDB Atlas ou Cloud MongoDB
# ============================================
# MongoDB Atlas (cloud) - substitua pelos seus valores reais
# MONGO_URI=mongodb+srv://username:password@cluster.mongodb.net/oficina2?retryWrites=true&w=majority

# Outros provedores de MongoDB cloud
# MONGO_URI=mongodb://username:password@host.exemplo.com:27017/oficina2?authSource=admin
